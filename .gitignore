.venv/
.state/
.artifacts/
__pycache__/
*.py[cod]
.DS_Store

.fixtures/
jobs/*/pilot-log.jsonl
jobs/*/flow-log.jsonl

# Frontend toolchain (the built bundle under apps/control-panel/static IS
# committed — the console must run with no JavaScript toolchain present).
node_modules/
